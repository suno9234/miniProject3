# AI Agent API

## 환경 설정

1. `.env.example`을 복사해서 `.env` 파일 생성:
```bash
cp .env.example .env
```

2. `.env` 파일에서 비밀번호 설정

## 실행

```bash
cd backend
docker-compose up --build
```

- API: http://localhost:8000
- DB: localhost:3306